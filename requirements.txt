# DESK — Python dependencies (pinned)
# Add per increment. Never add without flagging to Dhoni.

# D057 — DuckDB replaces BigQuery (dbt-duckdb replaces dbt-bigquery)
duckdb>=1.0.0
dbt-duckdb~=1.8.0
pyarrow>=16.1.0

# Increment 2-5 — Ingestion pipeline
requests==2.32.3
jsonschema==4.23.0

# Increment 6-8 — dbt transformations
dbt-core==1.8.7
