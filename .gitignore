# Secrets — Operator verifies these exist before first commit
.env
*service-account*.json
*-key.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/
.venv/
.pytest_cache/

# Node (frontend, documentary-site)
node_modules/

# Cricket Crew — runtime scoreboard + internal working notes, not project content
.crew-status
SESSION.md
AGENTS.md
session.log.md

# Candid internal prep material — private Artifact only, not for public repo
INTERVIEW_DEEP_DIVE.md

# dbt
dbt/target/
dbt/logs/
dbt/.user.yml

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
.vercel

# DuckDB — ephemeral per pipeline run, never committed
*.duckdb
data/desk.duckdb

# Parquet history files must NOT be gitignored (they are committed)
# data/history/*.parquet — intentionally tracked
