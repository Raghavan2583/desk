name: 'desk'
version: '1.0.0'
config-version: 2

profile: 'desk'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  desk:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
